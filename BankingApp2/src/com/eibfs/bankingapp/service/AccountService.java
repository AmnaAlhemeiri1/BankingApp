package com.eibfs.bankingapp.service;

public class AccountService {
///we will use the service to call the service methods.
	//just to call the service methods.
	//how many objects are required? -only one
	//how can i restrict the service to create only one object?
	//singleton DP.
	//DP: design pattern : common solution
	//common problem.
	//getting a singleton object(only one)
	//service and repo.
}
